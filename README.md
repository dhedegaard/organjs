# organjs

Play an organ in the browser. Click or drag across the keys, or use the Z and Q rows of your keyboard. Pull the drawbars down to add harmonics.

```sh
npm install
npm run dev
```

Built with Vite, React, TypeScript and the Web Audio API. See `BACKLOG.md` for what comes next.
